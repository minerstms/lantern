# Known Issues (Helper Knowledge — No Student Data)

This file is for the product-help assistant. It contains **no student names, IDs, or incident data**.

## Product Issues

- Store ledger: store role does not see incident narrative (note_text) for FERPA safety.
- Admin key: must be set once per device; no server-side session.
- Enrollment: device lock stored in localStorage; clearing browser data requires re-enrollment.

## Workflow Notes

- Escalation: teacher logs concern → optionally assigns student → sends to office → case created.
- School Response Team handles cases from secretary console or admin page.
- Principal exception email sent for Student Refused, Student Did Not Arrive, or manual Notify Principal.

## Safe Examples (Synthetic Only)

- Example student name: "Alex Smith" (synthetic).
- Example case ID: "TMS-2026-000001" (format only).
- Example teacher: "Ms. Johnson" (synthetic).

**Do not paste real student names, case IDs, or incident details into the helper.**
