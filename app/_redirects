# Cloudflare Pages may 302 /page.html → /page (“pretty URLs”). That breaks auth ?return= which
# expects real paths like /locker.html. These 301s send extensionless URLs back to the .html file
# so the final address bar (and pathname) match our static file names. Query strings are preserved
# by the client redirect chain per Pages behavior.
#
# Do NOT add /login → /login.html: that 301 can chain with Pages “pretty” handling and drop or
# break ?return= on the login URL. Auth uses /login.html?return=… directly; pilot-auth normalizes
# extensionless /login in return paths where needed.
#
# /admin routing (see docs in repo; do not use 301 /admin → /admin.html — fights “pretty URLs”):
# - Pages may redirect /admin.html → extensionless URL (often /admin) before _redirects.
# - Pages may normalize /admin ↔ /admin/ with 301/308 unless both forms are handled.
# - Use 200 proxy: /admin serves admin.html with NO Location header (breaks /admin↔/admin.html 301 loops).
# - Force ONE trailing-slash shape: /admin/ → /admin (301) so /admin/ never chains with other rules.
# Order: more specific /admin/ first, then /admin.

/ /login.html 200

/locker /locker.html 301
/admin/ /admin 301
/admin /admin.html 200
/teacher /teacher.html 301
/explore /explore.html 301
/games /games.html 301
/store /store.html 301
/change-password /change-password.html 301
